import { StyleSheet } from "react-native";
export const common = StyleSheet.create({
  container: {
    flex: 1,
    marginTop:20
  },
  centeredContainer: {
    flex: 1,
    alignItems: "center",
    justifyContent: "center",
  },
  rowConatiner: {
   flexDirection:"row"
  },
});